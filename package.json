{
  "name": "csp-new-gen-aqa",
  "version": "1.0.0",
  "description": "If you see errors about script execution policies (e.g., 'running scripts is disabled on this system'), you need to run the following command in PowerShell as Administrator:",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "test:report": "npx playwright show-report",
    "test:project": "npx playwright test --project=chromium",
    "allure:generate": "allure generate ./allure-results --clean -o ./allure-report",
    "allure:open": "allure open ./allure-report"
  },
  "repository": {
    "type": "git",
    "url": "https://gitlab.intra.hkma.gov.hk/common-infra/csp-new-gen-aqa.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@types/node": "^24.0.3",
    "allure-commandline": "^2.34.1",
    "allure-playwright": "^3.4.1",
    "dotenv": "^16.6.1"
  },
  "main": "playwright.config.js",
  "directories": {
    "test": "tests"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  }
}
